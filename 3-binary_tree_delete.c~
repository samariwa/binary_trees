#include "binary_trees.h"

int delete_node(binary_tree_t *node)
{
	if (node->left != NULL)
		delete_node(node->left);
	if (node->right != NULL)
		delete_node(node->right);

	free(node);

}

/**
 *
 */
void binary_tree_delete(binary_tree_t *tree)
{
	int deleted = delete_node(*tree);
}
